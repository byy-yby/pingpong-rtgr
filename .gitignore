# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# 虚拟环境
.venv/
venv/
env/

# 测试 / 缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 运行数据（标定结果、录制的图像序列）
data/recordings/
data/calibration/*.json
data/calibration/*.yaml
data/calibration/*.npz

# 用户保存的相机参数（曝光/增益/伽马，本地调试用）
config/camera_settings.json

# 图像临时文件
*.npy
*.bmp
*.jpg
*.png

# 模型权重 / 训练产物
*.pt
*.onnx
runs/

# SMPL 身体模型（smpl.is.tue.mpg.de 注册下载，非再分发，不进版本库）
data/bodymodels/

# Claude Code worktree（多 session 并行隔离，勿提交）
.claude/worktrees/

# 训练数据集（体量大，本地保留、不进版本库）
data/ball_dataset/
data/ball_dataset_aug/

# 下载残留 / 临时截图
*.part
ScreenCamera_*
ScreenCapture_*
